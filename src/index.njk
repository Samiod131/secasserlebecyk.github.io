---
layout: layouts/base.njk
title: Accueil
---

<section class="hero">
    <h1>Bienvenue sur Se Casser le Bec YK</h1>
    <p class="hero-subtitle">Un blog de mathématiques, science et réflexions</p>
</section>

<section class="recent-posts">
    <h2>Articles Récents</h2>
    <div class="post-grid">
        {% set postslist = collections.blog | reverse %}
        {% for post in postslist.slice(0, 6) %}
        <article class="post-card">
            {% if post.data.thumbnail %}
            <div class="card-thumbnail">
                <a href="{{ post.url | url }}">
                    <img src="{{ post.data.thumbnail | url }}" alt="{{ post.data.title }}">
                </a>
            </div>
            {% endif %}
            <div class="card-content">
                <h3><a href="{{ post.url | url }}">{{ post.data.title }}</a></h3>
                <div class="post-meta">
                    <time>{{ post.data.date | readableDate }}</time>
                    {% if post.data.section %}
                    <span class="separator">•</span>
                    <span class="section">{{ post.data.section }}</span>
                    {% endif %}
                </div>
                {% if post.data.description %}
                <p>{{ post.data.description }}</p>
                {% endif %}
                {% if post.data.tags %}
                <div class="post-tags">
                    {% for tag in post.data.tags %}
                    <span class="tag">#{{ tag }}</span>
                    {% endfor %}
                </div>
                {% endif %}
            </div>
        </article>
        {% endfor %}
    </div>
    <div class="text-center">
        <a href="{{ '/blog/' | url }}" class="button">Voir tous les articles →</a>
    </div>
</section>
